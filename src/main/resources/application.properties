# This project uses a native Java HTTP server rather than Spring configuration.
# Runtime settings are read from operating-system environment variables first,
# then from the repository-root .env file. See .env.example for supported keys.
